
	; This program will create a macro called setTo10
	; which will set numeric value given by user and pront it

	(defmacro setTO10(num))
	(setq num 15) (print num)  ;setq and defvar both are used for defining 					    ;variables
	(defvar a 30)
	(print a)
	(setTo10 x) ;calling macro setTo10
